let sum = (x, y) => {
  if (!Number(x) || !Number(y)) {
    return "Both inputs must be Numbers";
  } else return x + y;
};
let Subtr = (x, y) => {
  if (!Number(x) || !Number(y)) {
    return "Both inputs must be Numbers";
  } else return x - y;
};
